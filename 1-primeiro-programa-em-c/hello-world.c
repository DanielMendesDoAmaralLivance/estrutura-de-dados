#include <stdio.h>

// O c não inicializa variáveis. Exemplo: int a; print("%d"); retorna qualquer valor que estiver alocado na memória naquele instante, e não 0, como em outras linguagens.
// Por isso, sempre inicialize variáveis em C.

int main() {
    printf("Hello, World!");

    return 0;
}